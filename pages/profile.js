// import { getProfile } from "../utils";

const Profile = () => {
   // const { profile, isLoading, isError } = getProfile();

   return (
      <>
         <h1>Profile</h1>
      </>
   );
};

export default Profile;
